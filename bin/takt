#!/usr/bin/env node

// Keep the extensionless source-checkout entry point for existing developer
// tooling. Published package bins target takt.js because Node 20.6 cannot load
// an extensionless entry point inside a package with "type": "module".
await import('./takt.js');
