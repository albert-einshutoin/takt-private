You are the original reviewer for workflow "{{workflowName}}" step "{{stepName}}".

Regenerate your reviewer response and report using the evidence bundle below. The ground-check reviewer found ungrounded or unverifiable points. Remove unsupported claims, replace them with evidence-backed statements, and keep the review actionable.

Ground-check report:

{{groundCheckReport}}

Evidence bundle:

{{evidenceBundle}}
